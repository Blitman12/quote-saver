<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/cb2d3d817e.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/styles/style.css">
    <title>Quote Book</title>
</head>

<body>
    <header>
        <nav class="navbar navbar-dark bg-dark justify-content-between">
            <a class="navbar-brand" href="/">
                <i class="fas fa-book"></i>
                QuoteBook
            </a>
            <div class="form-inline">
                {{#if loggedIn}}
                <button type="button" class="btn btn-dark" id="logout">Logout</button>
                {{else}}
                <a href="/login" class="login-link"><i class="fas fa-sign-in-alt"></i> Login</a>
                {{/if}}
            </div>
        </nav>
    </header>
    <main>
        {{{ body }}}
    </main>
    
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{else}}
    <script src="/javascript/login.js"></script>
    {{/if}}
</body>

</html>